name: shell commands

on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
      - name: echo a string 
        run: echo "hellow world"
      - name: dish and memory
          free -h
          df -h
      - name: multiple script
        run: |
          node -v
          npm -v 